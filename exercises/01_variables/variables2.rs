fn main() {
    // i32 is the default (inferred) integer type
    // Can also write as:
    // let x = 10;
    let x: i32 = 10;

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
